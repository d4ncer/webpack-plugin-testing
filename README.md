# Webpack 2 plugin API exploration

## Dependency Graph builder

Generates a visualized dependency graph of your project. Requires `graphviz` to be installed (`brew install graphviz`, if you use Homebrew)

```
$ npm 
$ npm start
$ npm run graphviz
```


